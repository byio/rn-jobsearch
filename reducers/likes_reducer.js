// import action types
import { LIKE_JOB } from '../actions/types';

// export reducer
export default (state, action) => {
  switch (action.type) {
    case LIKE_JOB:
      
    default:
      return state;
  }
};
